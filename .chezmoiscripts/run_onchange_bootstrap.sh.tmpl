#!/bin/bash

# ansible/bootstrap.yml hash: {{ include "ansible/bootstrap.yml" | sha256sum }}

if command -v ansible-playbook >/dev/null 2>&1; then
  ansible-playbook --ask-become-pass {{ joinPath .chezmoi.sourceDir "ansible" "bootstrap.yml" | quote }} 
fi